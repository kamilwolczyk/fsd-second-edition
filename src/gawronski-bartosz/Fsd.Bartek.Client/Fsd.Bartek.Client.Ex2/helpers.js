var app = app|| {};

(function(){
    function schuffle(){
        var numbers = [1,3,0,2]

        //TODO: implements schuffle methods

        return numbers;       
    }

    app.helpers = {
        schuffle: schuffle
    }
}());