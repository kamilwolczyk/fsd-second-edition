@model int
@{
    ViewBag.Title = "Home";
}
@section PageTitle { Home page }

<div class="jumbotron">
    <h1>Hello!</h1>
    <p class="lead">
        We have @Model @(Model == 1 ? "product" : "products").
    </p>
</div>